import React, { useState, useEffect, useRef } from 'react';

const SearchButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      This is built in a separate app
    </button>
  );
}

  export default SearchButton;